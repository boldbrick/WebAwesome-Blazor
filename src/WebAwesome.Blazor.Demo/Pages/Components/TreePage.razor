@page "/components/tree"

<PageTitle>Tree</PageTitle>

<h1>Tree</h1>
<p><code>&lt;wa-tree&gt;</code> - <strong>wrapper not yet implemented</strong>; the example below uses the native element.</p>

<ExampleSection Title="Default" Code="@DefaultExample">
    <wa-tree></wa-tree>
</ExampleSection>

@* TODO: implement the WaTree wrapper, then replace the native element example *@

<ApiTable Tag="wa-tree" />

@code {
    private const string DefaultExample = "<wa-tree></wa-tree>";
}