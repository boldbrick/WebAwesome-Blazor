@page "/components/scroller"

<PageTitle>Scroller</PageTitle>

<h1>Scroller</h1>
<p><code>&lt;wa-scroller&gt;</code> - <strong>wrapper not yet implemented</strong>; the example below uses the native element.</p>

<ExampleSection Title="Default" Code="@DefaultExample">
    <wa-scroller></wa-scroller>
</ExampleSection>

@* TODO: implement the WaScroller wrapper, then replace the native element example *@

<ApiTable Tag="wa-scroller" />

@code {
    private const string DefaultExample = "<wa-scroller></wa-scroller>";
}