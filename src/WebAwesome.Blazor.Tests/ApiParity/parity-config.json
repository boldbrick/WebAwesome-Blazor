{
  "$comment": "Configuration for the API surface parity tests (ApiSurfaceParityTests, ElementMethodInvocationTests). 'enabled' is flipped to true by the wa-upgrade process once expected-api-surface.json matches the version being implemented. Overrides document intentional naming deviations of the Blazor wrappers - typically idiomatic .NET PascalCase word splits (e.g. maxlength -> MaxLength, autofocus -> AutoFocus) that differ from mechanical kebab-case conversion; ignores document intentional omissions (each ignore should carry a reason in 'ignoreReasons'). 'nativeElementMethods' lists DOM methods valid on any element; per-component 'extraElementMethods' allowlists element methods that exist in the Web Awesome source but are not CEM-documented - these MUST be re-verified against the target version's source on every upgrade (the CEM diff cannot see them).",
  "enabled": true,
  "targetWaVersion": "3.0.0",
  "globalIgnoredAttributes": [
    "title"
  ],
  "ignoredComponents": [],
  "componentClassOverrides": {
    "wa-textarea": "WaTextArea"
  },
  "nativeElementMethods": [ "focus", "blur", "click" ],
  "components": {
    "wa-details": {
      "eventOverrides": {
        "wa-show": "OnToggle",
        "wa-hide": "OnToggle",
        "wa-after-show": "OnAfterShow",
        "wa-after-hide": "OnAfterHide"
      }
    },
    "wa-checkbox": {
      "ignoredAttributes": [ "name", "form", "checked" ],
      "ignoredEvents": [ "change" ]
    },
    "wa-color-picker": {
      "ignoredAttributes": [ "name", "form" ],
      "ignoredEvents": [ "change" ]
    },
    "wa-input": {
      "attributeOverrides": {
        "maxlength": "MaxLength",
        "minlength": "MinLength",
        "autocapitalize": "AutoCapitalize",
        "autocorrect": "AutoCorrect",
        "autofocus": "AutoFocus",
        "enterkeyhint": "EnterKeyHint",
        "inputmode": "InputMode"
      },
      "ignoredAttributes": [ "form" ],
      "ignoredEvents": [ "change" ]
    },
    "wa-button": {
      "attributeOverrides": {
        "formaction": "FormAction",
        "formenctype": "FormEncType",
        "formmethod": "FormMethod",
        "formnovalidate": "FormNoValidate",
        "formtarget": "FormTarget"
      }
    },
    "wa-zoomable-frame": {
      "attributeOverrides": {
        "srcdoc": "SrcDoc",
        "allowfullscreen": "AllowFullScreen",
        "referrerpolicy": "ReferrerPolicy"
      },
      "extraElementMethods": [ "setZoom", "reset" ]
    },
    "wa-select": {
      "ignoredAttributes": [ "name", "form" ],
      "ignoredEvents": [ "change" ]
    },
    "wa-slider": {
      "attributeOverrides": {
        "autofocus": "AutoFocus"
      },
      "ignoredAttributes": [ "name", "form" ],
      "ignoredEvents": [ "change" ]
    },
    "wa-switch": {
      "ignoredAttributes": [ "name", "form" ],
      "ignoredEvents": [ "change" ]
    },
    "wa-radio": {
      "ignoredAttributes": [ "form" ]
    },
    "wa-radio-group": {
      "ignoredEvents": [ "change" ]
    },
    "wa-textarea": {
      "attributeOverrides": {
        "maxlength": "MaxLength",
        "minlength": "MinLength",
        "autocapitalize": "AutoCapitalize",
        "autocorrect": "AutoCorrect",
        "autofocus": "AutoFocus",
        "enterkeyhint": "EnterKeyHint",
        "inputmode": "InputMode"
      },
      "ignoredAttributes": [ "name", "form" ],
      "ignoredEvents": [ "change" ]
    },
    "wa-rating": {
      "ignoredAttributes": [ "getSymbol" ],
      "ignoredEvents": [ "change" ]
    },
    "wa-carousel": {
      "ignoredAttributes": [ "currentSlide", "slides" ]
    },
    "wa-dropdown-item": {
      "ignoredAttributes": [ "submenuOpen" ]
    },
    "wa-dialog": {
      "extraElementMethods": [ "show", "hide" ]
    },
    "wa-drawer": {
      "extraElementMethods": [ "show", "hide" ]
    },
    "wa-dropdown": {
      "extraElementMethods": [ "show", "hide", "reposition" ]
    },
    "wa-copy-button": {
      "extraElementMethods": [ "copy" ]
    },
    "wa-include": {
      "extraElementMethods": [ "reload" ]
    },
    "wa-mutation-observer": {
      "extraElementMethods": [ "stopObserver", "startObserver", "takeRecords" ]
    },
    "wa-resize-observer": {
      "extraElementMethods": [ "stopObserver", "startObserver" ]
    },
    "wa-popover": {
      "extraElementMethods": [ "reposition" ]
    },
    "wa-relative-time": {
      "extraElementMethods": [ "update" ]
    },
    "wa-tooltip": {
      "extraElementMethods": [ "reposition" ]
    }
  },
  "ignoreReasons": {
    "title": "native HTML attribute, passed through via AdditionalAttributes",
    "change": "native change event folded into Blazor two-way binding (ValueChanged / @bind-Value)",
    "name": "form-control name is handled by Blazor InputBase form integration (NameAttributeValue / EditContext), not a discrete parameter",
    "form": "form association is handled by Blazor EditForm / EditContext, not a discrete attribute parameter",
    "checked": "checkbox checked state is the two-way-bound Value on WaInputBase<bool>",
    "maxlength": "exposed as MaxLength on WaInputBase (idiomatic .NET casing); mapped via attributeOverrides",
    "minlength": "exposed as MinLength on WaInputBase (idiomatic .NET casing); mapped via attributeOverrides",
    "getSymbol": "function-valued property (custom symbol renderer callback), not an authorable HTML attribute",
    "currentSlide": "read-only JS state property of wa-carousel, not an authorable HTML attribute",
    "slides": "read-only JS collection property of wa-carousel, not an authorable HTML attribute",
    "submenuOpen": "read-only JS state property of wa-dropdown-item, not an authorable HTML attribute",
    "extraElementMethods:show/hide": "dialog/drawer/dropdown/select-like open-close methods exist on the elements (verified against the WA 3.0.0 source) but are not CEM-documented for these components",
    "extraElementMethods:reposition": "popup repositioning method present on wa-dropdown/wa-popover/wa-tooltip source, not CEM-documented",
    "extraElementMethods:copy": "wa-copy-button trigger method present in source, not CEM-documented",
    "extraElementMethods:reload": "wa-include refetch method present in source, not CEM-documented",
    "extraElementMethods:stopObserver/startObserver/takeRecords": "observer control methods confirmed from the WA 3.0.0 source after the disconnect/reconnect rename incident; the CEM reports empty methods for both observers, so renames here are invisible to the upgrade diff - re-verify each upgrade",
    "extraElementMethods:update": "wa-relative-time refresh method present in source, not CEM-documented",
    "extraElementMethods:setZoom/reset": "wa-zoomable-frame zoom control methods present in source, not CEM-documented"
  }
}
